name: "Build container image"
description: "Action for building a container image, or restoring it from
  cache if such exists."
author: "The Dev Nolus Team <dev@nolus.io>"

outputs:
  image-hash:
    description: ""
    value: |-
      ${{ steps.check-lockfiles.outputs.image-hash }}
  image-id:
    description: ""
    value: |-
      ${{ steps.check-lockfiles.outputs.image-id }}

runs:
  using: "composite"
  steps:
    - id: "rust-stable"
      uses: "./.github/actions/rust-stable-container"
    - env:
        RUST_ID: |-
          ${{ steps.rust-stable.outputs.image-id }}
      id: "check-lockfiles"
      uses: "./.github/actions/build-container"
      with:
        additional-digest-data: |-
          ${{ steps.rust-stable.outputs.image-hash }}
        additional-digest-targets: |-
          ./ci/images/check-lockfiles.Containerfile
        build-step: |-
          "build" \
            "check-lockfiles" \
            "check-lockfiles" \
            -- \
            --build-arg "rust_id=${RUST_ID:?}" \
            --tag "localhost/local/check-lockfiles"
        disable-caching: true
        image-friendly-name: "check-lockfiles"
        image-name: "localhost/local/check-lockfiles"
