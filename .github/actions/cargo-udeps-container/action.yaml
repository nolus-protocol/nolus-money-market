name: "Build container image"
description: "Action for building a container image, or restoring it from
  cache if such exists."
author: "The Dev Nolus Team <dev@nolus.io>"

outputs:
  image-hash:
    description: ""
    value: |-
      ${{ steps.cargo-each.outputs.image-hash }}
  image-id:
    description: ""
    value: |-
      ${{ steps.cargo-each.outputs.image-id }}

runs:
  using: "composite"
  steps:
    - id: "rust-stable"
      uses: "./.github/actions/rust-stable-container"
    - env:
        RUST_ID: |-
          ${{ steps.rust-stable.outputs.image-id }}
      id: "cargo-each"
      uses: "./.github/actions/build-container"
      with:
        additional-digest-data: |-
          ${{ inputs.rust-stable-image-hash }}
        additional-digest-targets: |-
          ./ci/images/cargo-udeps.Containerfile
        build-step: |-
          "build" \
            "cargo-udeps" \
            "cargo-udeps" \
            -- \
            --build-arg "rust_id=${RUST_ID:?}" \
            --tag "localhost/local/cargo-udeps"
        image-friendly-name: "cargo-udeps"
        image-name: "localhost/local/cargo-udeps"
