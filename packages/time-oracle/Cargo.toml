[package]
name = "time-oracle"
version = "0.1.0"
authors = ["Vladimir Kolesnikov <v.kolesnikov@nolus.io>"]
edition = "2021"

[dependencies]
platform = { path = "../../packages/platform" }
sdk = { path = "../../packages/sdk", features = ["storage"] }

serde = { version = "1.0.145", default-features = false, features = ["derive"] }
thiserror = "1.0.37"
