use serde::{Deserialize, Serialize};

use finance::currency::{Currency, SymbolStatic};

#[derive(Debug, Copy, Clone, Ord, Eq, PartialOrd, PartialEq, Serialize, Deserialize)]
/// Name: #name#
pub struct #normalized_ticker#;

impl Currency for #normalized_ticker# {
    const TICKER: SymbolStatic = "#ticker#";
}
